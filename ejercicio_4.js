// Perímetro de un cuadrado

// Ingresar valor
let lado = prompt("Ingresa el valor de cada lado: ");
lado = parseFloat(lado);
alert(`Cada lado mide: ${lado} unidades`);

// Calcular perímetro
let perimetro = lado * 4;

// Mostrar resultados
alert(`El área es: ${perimetro} unidades`);
